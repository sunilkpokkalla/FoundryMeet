name: FoundryMeet
options:
  bundleIdPrefix: com.startupnetwork
settings:
  base:
    # Xcode 26 Explicit Modules fails on FirebaseFirestoreInternal binary headers
    # ("module 'Foundation' is needed but has not been provided").
    SWIFT_ENABLE_EXPLICIT_MODULES: NO
    CLANG_ENABLE_EXPLICIT_MODULES: NO
targets:
  FoundryMeet:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources: [FoundryMeet]
    dependencies:
      - package: Firebase
        product: FirebaseAnalytics
      - package: Firebase
        product: FirebaseAuth
      - package: Firebase
        product: FirebaseCore
      - package: Firebase
        product: FirebaseFirestore
    settings:
      base:
        SWIFT_ENABLE_EXPLICIT_MODULES: NO
        CLANG_ENABLE_EXPLICIT_MODULES: NO
        CODE_SIGN_ENTITLEMENTS: FoundryMeet/FoundryMeet.entitlements
    entitlements:
      path: FoundryMeet/FoundryMeet.entitlements
      properties:
        com.apple.developer.applesignin: [Default]
    info:
      path: FoundryMeet/Info.plist
      properties:
        UILaunchScreen: {}
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - com.googleusercontent.apps.557443174588-e6d3cslgqabaivuh1ib8d2spu11un4er
packages:
  Firebase:
    url: https://github.com/firebase/firebase-ios-sdk.git
    from: 12.16.0
